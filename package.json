{
  "name": "vless-worker-cdn",
  "version": "1.0.0",
  "description": "VLESS Worker with CDN, Reverse Domain, and Proxy Health Check",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler build"
  },
  "dependencies": {
    "wrangler": "^3.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231218.0"
  }
}